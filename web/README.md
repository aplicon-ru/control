# web/

React UI, embedded into the `ukon` binary at build time via `go:embed`.
Not yet scaffolded — pulls shared components from `@univerkon/*`
(`univerkon-ui` repo) once that exists. See spec §16.3 and §12.
