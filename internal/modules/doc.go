// Package modules implements the module catalog, version tracking,
// schema-driven configuration forms, and drift detection between the
// config held by CP and what is actually deployed. See spec §5.2.
package modules
