// Package auth implements local and OIDC authentication, JWT sessions,
// optional TOTP 2FA, IP allowlisting, and role-based access control
// (super_admin / org_admin / operator / viewer). See spec §3.
package auth
