// Package updates implements the update watcher: periodic manifest
// polling, version diffing against installed modules, and notification
// triggers. Applying an update is always a manual, operator-confirmed
// action — see spec §5.5.
package updates
