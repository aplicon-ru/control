# modules/

`module.yaml` manifests for each -кон (compose template ref, vars
schema, healthcheck, deps, demo_mode). See spec §5.2 for the manifest
shape and §16.1 for the dependency graph between Core, Шлюз, and
browser-facing fronts.

Empty for now — module manifests get added as each -кон's own spec
stabilizes (see spec §15).
